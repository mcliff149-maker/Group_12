# Copy this to .env.local and fill in the value to use the Django backend.
# Leave unset (or remove the file) to use the localStorage fallback.

# Django backend (run with: cd Backend && python manage.py runserver 8000)
VITE_API_BASE_URL=http://localhost:8000/api
